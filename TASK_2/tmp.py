def run_cmd(): # I will do that later
    pass

# prompt = input("prompt: ")

# cmd=f"{prompt if prompt else 'prompt$/>'}{run_cmd()}"
# x = cmd.format(run_cmd)

bla = '.__globals__'
test = '{bla}NONEbaablblalalbalba'
y = test.format(run_cmd)

print()